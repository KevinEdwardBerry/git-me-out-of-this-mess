public class GetThing : IScalar<Thing>
{
    public GetThing(Guid id)
    {
        Id = id;

        q => q.AsQueryable<Thing>(Tracked)
            .Where(t => t.Id == Id);
    }

    public Guid Id      { get; private set; }
    public bool Tracked { get; set; }
}
